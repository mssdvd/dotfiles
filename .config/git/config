[alias]
	# TODO: Improve alignment
	l = log --pretty='%C(yellow)%h %<(15)%C(cyan)%ad %<(20,trunc)%Cblue%an%C(auto)%d %Creset%s' --date=relative --date-order --graph
[user]
	name = Davide Masserut
	email = dm@mssdvd.com
[sendemail]
	smtpserver = smtp.migadu.com
	smtpuser = dm@mssdvd.com
	smtpencryption = tls
	smtpserverport = 587
	# smtpPass = **PASSWORD**
[core]
	editor = emacsclient -t -a ''
[commit]
	verbose = true
[grep]
	lineNumber = true
[color "grep"]
	filename = magenta
	linenumber = green
[github]
	user = mssdvd
[credential]
	helper = cache --timeout=5400
[filter "remove-smtpPass"]
	clean = "sed -e 's/\tsmtpPass.*/\t# smtpPass = **PASSWORD**/'"
[filter "remove-msmtp-password"]
	clean = "sed -e 's/^password .*/# password **PASSWORD**/'"
[filter "remove-mbsync-Pass"]
	clean = "sed -e 's/^Pass .*/# Pass **PASSWORD**/'"
